[paths]
# Folder containing calendar.csv, sell_prices.csv, sales_train_evaluation.csv
m5_input_dir = "data"

[local.lgbm]
horizon = 28
start_day = 1000
seed = 42

lags = [28, 35, 42, 49]
rolling_shift = 28
rolling_windows = [7, 28, 49]

# Tuning (Optuna)
tune = true
tune_frac = 0.1
n_trials = 15
nfold = 3
num_boost_round_cv = 500
study_name = "M5_LGBM_Local"

# Final training (LightGBM)
objective = "tweedie"
metric = "rmse"
num_boost_round = 1500
early_stopping_rounds = 100
log_evaluation = 50

[local.nbeatsx]
horizon = 28
start_day = 1000
alias = "NBEATSx_MQLoss_Local"
log_name = "nbeats_mqloss_local"
prefer_mps = true

freq = "D"
input_size = 364
scaler_type = "robust"
max_steps = 3500
val_size = 28
val_check_steps = 12
early_stop_patience_steps = 20
dropout_prob_theta = 0.2
batch_size = 512
devices = 1
enable_progress_bar = false
exclude_insample_y = false
keep_full_history = false

learning_rate = 0.0001
quantiles = [0.5, 0.8, 0.9]

# Local CA-FOODS uses snap_CA rather than snap_active
futr_exog_list = ["sell_price", "is_available", "snap_CA", "is_event"]
